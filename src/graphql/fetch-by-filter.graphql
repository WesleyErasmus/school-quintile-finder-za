query FilterSchools($filters: SchoolFilters) {
  filterSchools(filters: $filters) {
    count
    schools {
      id
      name
      quintile
      province
      phase
      sector
      fee_paying
      address
    }
  }
}
