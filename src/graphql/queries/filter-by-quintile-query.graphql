query FilterQuintiles($selectedFilter: String!) @cached {
  schools_aggregate(where: { quintile: { _eq: $selectedFilter } }) {
    aggregate {
      count
    }
  }
  schools(
    where: { quintile: { _eq: $selectedFilter } }
    order_by: { name: asc }
  ) {
    id
    name
    quintile
    province
    phase
    sector
    fee_paying
    address
  }
}
