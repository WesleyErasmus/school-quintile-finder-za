query SearchSchools($searchTerm: String!) @cached {
  schools(where: { name: { _ilike: $searchTerm } }, limit: 10) {
    id
    name
    quintile
    province
    phase
    sector
    address
    fee_paying
  }
}